apiEndpoints:
  api:
      host: '*.com'

serviceEndpoints:
  backend:
      url: 'http://localhost:5998'

policies:
  - proxy

pipelines:
  apiPipeline:
    apiEndpoints:
      - api
    policies:
      - proxy:
        - action:
            serviceEndpoint: backend
http:
  port: 5997
