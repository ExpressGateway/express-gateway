apiEndpoints:
  users:
    host: '*'
    paths:
      - /users/*
http:
  port: 8080
https:
  port: 9999
  tls: {}
serviceEndpoints:
  backend:
    url: 'http://www.example.com'
pipelines:
  - name: ecommerce
    apiEndpoints:
      - users
    policies:
      - proxy:
          - condition:
              name: always
            action:
              name: proxy
              serviceEndpoint: backend
              param2: test
